services:
  - type: worker
    name: trading-signal-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python signal_bot.py"
    envVars:
      - key: BOT_TOKEN
        sync: false
